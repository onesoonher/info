# Agent 更新守則（本專案）

**最後更新：** 2026-04-04

本文件供 AI／開發者維護 **何萬順教授個人網站**（靜態 HTML、`css/main.css`、`js/main.js`、GitHub Pages）時遵循。變更前請先讀本節，**只改與任務相關的檔案**，避免連鎖破版。

**撰寫重點**：以下以**內容正確性、用語一致、檔案與連結可開**為主；版面／樣式若非必要不必改動，亦無須在文件中冗長描述。

---

## 1. 專案結構速覽

| 路徑 | 說明 |
|------|------|
| `index.html` | 首頁 |
| `experience.html` | 學歷經歷 |
| `research.html` | 學術研究（獎項、研究計畫、Modal 等） |
| `publications.html` | 出版著作列表與搜尋 |
| `css/main.css` | 全站樣式 |
| `js/main.js` | 全站腳本（導航、捲動等） |
| `publication/` | 著作 PDF；**連結路徑與實際檔名須一致** |
| `img/` | 圖片（Logo、圖示、機構標章等） |

部署網址：`https://onesoonher.github.io/info/index.html`（見 `readMe.md`）。

---

## 2. 內容更新（通用注意事項）

- **單一來源**：同一則資訊不要在多個 HTML 重複貼不同版本；若首頁與研究頁都有提及，更新時應**一併對齊**。
- **機構／計畫名稱**：與官方現行名稱一致（例如國科會與 **NSTC** 英文全稱、年份、獎項正式名稱）；中英並列時與既有區塊格式一致。
- **連結**：`href` 使用**相對路徑**（利於 GitHub Pages）；外部連結加 `target="_blank"` 時，比照既有寫法（若站內有 `rel` 等一併維持）。
- **改檔名或路徑後**：全站搜尋舊檔名／舊 URL，**全部替換**，避免 404。
- **可及性**：圖片補上有意義的 `alt`；裝飾用圖可 `alt=""`。
- **結構**：沿用既有 HTML 區塊結構（標籤階層、class）；**不要**為了微調文字而刪掉包住內容的父層，以免摺疊區塊／Modal／表格腳本失效。

---

## 3. 新增／更新「獎項」等（`research.html`）

獎項列於 **`research.html`** 上方獎項區，採 **摺疊區塊（accordion）**；新增或修改時以**內容**為主，**複製鄰近一則獎項的整段結構**最保險。

### 3.1 建議檢核項目

- **標題**：中文獎項名稱、主辦單位（教育部、國科會／NSTC、學校等）與**屆別／年份**正確；若為「最新一屆」，與頒獎公告或客戶提供文字一致。
- **英文對照**：`eg-awards` 內英文與按鈕上中文語意對應；專有名詞（獎項全名、機構名）與既有條目或官方英文一致。
- **細節段落**：展開區內的 `awards-subtitle`、內文、`eg-awards` 段落—**年份、屆次、獎項完整名稱**勿與標題矛盾；同一獎項多屆得獎時，**排序與格式**比照該區既有條目（通常較新在上或依年份排列）。
- **連結**：若提供官方新聞、學會公告，`href` 正確且可開；無連結則維持與他則相同的呈現方式（勿留空錨點語法錯誤）。
- **註解**：可為該則加上 HTML 註解（如 `<!-- 某某獎 -->`）便於日後搜尋。
- **不要**：隨意改 accordion／`button` 的 class 或巢狀結構，除非確認與頁尾 Bootstrap／自訂腳本相容。

### 3.2 研究計畫、國科會計畫敘述等

- 計畫名稱、年度、補助機關、計畫編號（若有）與客戶／計畫書一致。
- 機構 Logo 檔名與路徑（如 **`img/NSTC.svg`**）變更時，**HTML 的 `src` 與 `alt`** 一併更新；客戶指定保留之檔案勿刪。

---

## 4. 新增／更新「著作 PDF」與 `publications.html`

### 4.1 檔案與命名

- PDF 放在 **`publication/`** 根目錄。
- **檔名與連結**：GitHub Pages **大小寫敏感**；`href` 與實際檔名**逐字相同**（含空白、底線、全形字元）。
- **版本字尾**：Post-print、Publisher Version 等依既有慣例延用。

#### 檔名通式

```text
{前綴}{序號}-{簡短描述}.pdf
```

- **前綴 A–E**：對應著作類型（見下表），且須與該列 **`pub-catagory`** 一致。
- **序號**：**各前綴獨立編號**，新條目接續該前綴之**現有最大序號**，勿重複。

| 前綴 | 頁面類別（中文） | Category（英文） |
|:----:|-----------------|------------------|
| **A** | 期刊論文 | Refereed Journal Articles |
| **B** | 書目 | Bibliographies |
| **C** | 專書篇章 | Book Chapters |
| **D** | 專書 | Books and Monographs |
| **E** | 會議論文 | Conference Papers |

- **會議論文（E）**：許多條目無 PDF，屬正常；有檔案再上傳並補連結。
- 頁內註解範本（`A00`、`B00`…）與 **`publications.html`** 各區塊對應。

### 4.2 HTML 與檢查

- 新增列時比照既有 `<tr>` 欄位；更新後**實際點一次下載連結**確認無 404。
- 動到表格時，確認字元搜尋／類別篩選仍正常（見頁尾 script）。

### 4.3 Git 與大檔

- PDF 體積大，提交前確認是否應納入版本庫。
- 勿提交 `.DS_Store` 等系統檔。

---

## 5. 版面與樣式（僅供必要時）

- 樣式集中於 **`css/main.css`**。若僅更新文字內容，**通常不必改 CSS**。
- 若確需調整研究頁外觀，請先搜尋既有規則，並以**限定於該區的 class** 修改，避免影響其他頁相同元件。

---

## 6. 檢查清單（內容導向）

- [ ] 新／改動之文字與客戶或公開來源一致（年份、名稱、連結）。
- [ ] PDF、圖片路徑已更新且可開啟。
- [ ] `publications.html` 前綴與 Category 一致；下載連結無 404。
- [ ] 未引入不必要的系統檔或本機絕對路徑。

---

## 7. 本文件維護

- 專案慣例變更時**同步更新本檔**。
- **每次修訂本檔時，請更新文首「最後更新」日期**（建議 `YYYY-MM-DD`）。
- 結構性新規則若可重複使用，請**直接補進本檔**。
